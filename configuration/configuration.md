# Configure a deno project

## The config file

Deno will automatically look for a deno.json or deno.jsonc file inside your code folder.
You can specify a config file with deno run by supplying the --config parameter.

